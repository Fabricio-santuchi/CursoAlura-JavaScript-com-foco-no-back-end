// Utilize o método forEach para imprimir cada elemento de um array juntamente com seu índice.

const array = ["Joao", "Renan", "Fabricio", "Dvanie"];

array.forEach((elemento, i) => {
  console.log(`O indice é [${i}] o nome é [${elemento}]`);
});
