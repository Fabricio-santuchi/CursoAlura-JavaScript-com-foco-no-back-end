const salaJS = ['Evaldo', 'Camis', 'Mari'];
const salaPython = ['Ju', 'Leo', 'Raquel'];

const array = salaJS.concat(salaPython);

console.log(array);

/*
    concat();
    const array = arrayAleatorio.concat(OutroArrayAleatorio);
    array = (salaJS) + (salaPython);
    no final fica as duas array dentro de uma só (array).

    o concat serve para juntar 2(ou mais) array dentro de 1 só!
*/